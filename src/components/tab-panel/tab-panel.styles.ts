import {StyleSheet} from 'react-native';
import {SPACING} from '../../themes';

export const tabPanelStyles = StyleSheet.create({
  container: {
    flexDirection: 'row',
    alignItems: 'center',
    paddingHorizontal: SPACING.container,
  },
});
