import React from 'react';
import {Text, View} from 'react-native';

const MenuDrawer = () => {
  return (
    <View style={{width: 256, backgroundColor: '#ffffff00'}}>
      <Text>Drawer</Text>
    </View>
  );
};

export default MenuDrawer;
