import React from 'react';
import {Image} from 'react-native';
import headingImg from './../../images/heading.png';

const Header = () => {
  return <Image source={headingImg} />;
};

export default Header;
