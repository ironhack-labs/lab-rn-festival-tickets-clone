import {StyleSheet} from 'react-native';

export const eventOrganizerStyles = StyleSheet.create({
  organizer: {
    fontSize: 14,
    lineHeight: 21,
    fontWeight: '700',
    color: '#333333',
  },
});
