import {StyleSheet} from 'react-native';
import {SPACING} from '../../../themes';

export const similarEventTitleStyles = StyleSheet.create({
  text: {
    fontSize: 20,
    marginHorizontal: SPACING.container,
  },
});
