import {StyleSheet} from 'react-native';
import {SPACING} from '../../../themes';

export const similarEventCarrouselStyles = StyleSheet.create({
  content: {
    gap: 15,
    paddingHorizontal: SPACING.container,
    marginVertical: 15,
  },
});
